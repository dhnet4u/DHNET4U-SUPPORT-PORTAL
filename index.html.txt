<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>DHNET4U | Support Portal</title>
  
  <!-- Resource Hints for Maximum Performance -->
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link rel="preload" href="https://cdn.tailwindcss.com" as="script">
  <link rel="preload" href="https://fonts.googleapis.com/css2?family=Plus+Jakarta+Sans:wght@400;600;700;800&display=swap" as="style">
  <link rel="dns-prefetch" href="https://script.google.com">
  
  <script src="https://cdn.tailwindcss.com"></script>
  <link href="https://fonts.googleapis.com/css2?family=Plus+Jakarta+Sans:wght@400;600;700;800&display=swap&font-display=swap" rel="stylesheet">
  <style>
    /* Performance: Enable Hardware Acceleration for smooth animations */
    * { backface-visibility: hidden; -webkit-font-smoothing: antialiased; }
    
    body { 
      font-family: 'Plus Jakarta Sans', sans-serif; 
      background-color: #f0fdf4; /* Very light green bg */
      text-rendering: optimizeSpeed; /* Faster rendering */
      color: #1a1a1a; 
    }
    /* Mobile-first text optimization */
    @media (max-width: 640px) {
      body { font-size: 16px; } /* Ensure base font size is legible on mobile */
      .glass-card { padding: 1.5rem !important; }
      h1 { font-size: 3.5rem !important; line-height: 1 !important; }
      p { font-size: 0.95rem !important; line-height: 1.5 !important; }
      .report-tab { font-size: 0.85rem !important; padding: 0.75rem 0.5rem !important; }
      label { font-size: 0.85rem !important; font-weight: 700 !important; }
      input, select, textarea { font-size: 1rem !important; } /* Prevents iOS zoom on focus */
    }
    /* Mobile-first text optimization */
    @media (max-width: 640px) {
      body { font-size: 16px; } /* Ensure base font size is legible on mobile */
      .glass-card { padding: 1.5rem !important; }
      h1 { font-size: 3.5rem !important; line-height: 1 !important; }
      p { font-size: 0.95rem !important; line-height: 1.5 !important; }
      .report-tab { font-size: 0.85rem !important; padding: 0.75rem 0.5rem !important; }
      label { font-size: 0.85rem !important; font-weight: 700 !important; }
      input, select, textarea { font-size: 1rem !important; } /* Prevents iOS zoom on focus */
    }
    .mesh-gradient {
      background: radial-gradient(at 0% 0%, rgba(16, 185, 129, 0.15) 0px, transparent 50%),
                  radial-gradient(at 100% 0%, rgba(5, 150, 105, 0.15) 0px, transparent 50%),
                  radial-gradient(at 100% 100%, rgba(4, 120, 87, 0.1) 0px, transparent 50%),
                  radial-gradient(at 0% 100%, rgba(16, 185, 129, 0.15) 0px, transparent 50%);
      will-change: background;
    }
    .glass-card {
      background: rgba(255, 255, 255, 0.85);
      backdrop-filter: blur(20px);
      -webkit-backdrop-filter: blur(20px);
      border: 1px solid rgba(255, 255, 255, 0.4);
      box-shadow: 0 25px 50px -12px rgba(6, 95, 70, 0.1);
    }
    .pro-green-btn {
      background: linear-gradient(135deg, #059669 0%, #047857 100%);
      transition: all 0.2s cubic-bezier(0.4, 0, 0.2, 1);
      box-shadow: 0 10px 15px -3px rgba(16, 185, 129, 0.3);
      will-change: transform, box-shadow;
    }
    .pro-green-btn:hover {
      transform: translateY(-2px);
      box-shadow: 0 20px 25px -5px rgba(16, 185, 129, 0.4);
      filter: brightness(1.1);
    }
    .input-field {
      transition: all 0.15s ease;
      background: rgba(255, 255, 255, 0.9);
      will-change: transform, border-color, box-shadow;
    }
    .input-field:focus {
      background: #ffffff;
      border-color: #10b981;
      box-shadow: 0 0 0 4px rgba(16, 185, 129, 0.15);
      transform: translateY(-1px);
    }
    .floating-icon {
      filter: drop-shadow(0 4px 6px rgba(16, 185, 129, 0.2));
      will-change: filter;
    }
    @keyframes blob {
      0% { transform: translate(0px, 0px) scale(1); }
      33% { transform: translate(30px, -50px) scale(1.1); }
      66% { transform: translate(-20px, 20px) scale(0.9); }
      100% { transform: translate(0px, 0px) scale(1); }
    }
    .animate-blob {
      animation: blob 7s infinite alternate;
      will-change: transform, opacity;
      transform: translateZ(0); /* Hardware Acceleration */
    }
    .animation-delay-2000 { animation-delay: 2s; }
    
    @keyframes gradient-x {
      0%, 100% { background-size: 200% 200%; background-position: left center; }
      50% { background-size: 200% 200%; background-position: right center; }
    }
    .animate-gradient-x {
      animation: gradient-x 5s ease infinite;
      will-change: background-position;
    }
    
    .report-tab {
      background: white;
      border-color: #e5e7eb;
      color: #6b7280;
      transition: all 0.15s cubic-bezier(0.4, 0, 0.2, 1);
      will-change: transform, background-color, border-color, box-shadow;
    }
    .report-tab.active {
      background: #ecfdf5;
      border-color: #10b981;
      color: #065f46;
      box-shadow: 0 10px 15px -3px rgba(16, 185, 129, 0.1);
    }
    .animate-fade-in {
      animation: fadeIn 0.3s ease-out;
    }
    @keyframes fadeIn {
      from { opacity: 0; transform: translateY(10px); }
      to { opacity: 1; transform: translateY(0); }
    }
  </style>
</head>
<body class="min-h-screen flex items-center justify-center p-4 md:p-12 mesh-gradient relative overflow-x-hidden">

  <!-- World-Class Assistant Notification Modal -->
  <div id="pro-assistant-modal" class="fixed inset-0 z-50 flex items-center justify-center px-4 bg-emerald-950/40 backdrop-blur-sm hidden opacity-0 transition-opacity duration-500">
    <div class="bg-white rounded-[2.5rem] shadow-2xl max-w-md w-full overflow-hidden transform scale-95 transition-transform duration-500 border border-emerald-50">
      <div class="bg-emerald-600 p-8 text-center relative">
        <div class="absolute top-0 left-0 w-full h-full opacity-10 pointer-events-none">
          <svg class="w-full h-full" viewBox="0 0 100 100" preserveAspectRatio="none"><path d="M0 100 C 20 0 50 0 100 100 Z" fill="white"></path></svg>
        </div>
        <div class="inline-flex items-center justify-center w-20 h-20 bg-white/20 rounded-3xl backdrop-blur-md mb-4 animate-pulse">
          <svg class="w-10 h-10 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2.5" d="M13 16h-1v-4h-1m1-4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z"></path></svg>
        </div>
        <h3 class="text-2xl font-extrabold text-white tracking-tight">DHNET4U ASSISTANT</h3>
      </div>
      <div class="p-8 space-y-6">
        <div class="space-y-4">
          <div class="flex items-start space-x-4">
            <div class="mt-1 w-6 h-6 bg-emerald-100 rounded-lg flex items-center justify-center shrink-0">
              <span class="text-emerald-600 font-bold text-xs">01</span>
            </div>
            <p class="text-gray-700 font-medium text-sm leading-relaxed">
              Bundle reports should <span class="text-emerald-700 font-bold">only be made after 4 hours</span>, as stated in the SMS each client receives.
            </p>
          </div>
          <div class="flex items-start space-x-4">
            <div class="mt-1 w-6 h-6 bg-emerald-100 rounded-lg flex items-center justify-center shrink-0">
              <span class="text-emerald-600 font-bold text-xs">02</span>
            </div>
            <div class="space-y-3">
              <p class="text-gray-700 font-medium text-sm leading-relaxed">
                Except in cases of faster deliveries, please <span class="text-emerald-700 font-bold underline">always follow updates</span> on the DHNET4U WhatsApp channel for the latest status.
              </p>
              <a href="https://whatsapp.com/channel/0029Vb5bHB530LKLpLZRwz0y" target="_blank" class="inline-flex items-center space-x-2 bg-emerald-50 text-emerald-700 px-4 py-2 rounded-xl border border-emerald-100 hover:bg-emerald-100 transition-colors group">
                <svg class="w-4 h-4" fill="currentColor" viewBox="0 0 24 24"><path d="M.057 24l1.687-6.163c-1.041-1.804-1.588-3.849-1.587-5.946.003-6.556 5.338-11.891 11.893-11.891 3.181.001 6.167 1.24 8.413 3.488 2.246 2.248 3.484 5.232 3.483 8.413-.003 6.557-5.338 11.892-11.893 11.892-1.997-.001-3.951-.5-5.688-1.448l-6.308 1.654zm6.233-3.803c1.491.885 3.012 1.34 4.603 1.34 5.281 0 9.577-4.296 9.58-9.58 0-2.561-1.002-4.97-2.822-6.791s-4.229-2.816-6.786-2.816c-5.281 0-9.577 4.297-9.58 9.581 0 1.745.474 3.407 1.371 4.821l-1.031 3.766 3.865-1.012z"/></svg>
                <span class="font-bold text-xs uppercase tracking-wider">Tap for Latest Updates</span>
                <svg class="w-3 h-3 group-hover:translate-x-1 transition-transform" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2.5" d="M14 5l7 7m0 0l-7 7m7-7H3"></path></svg>
              </a>
            </div>
          </div>
        </div>
        <button onclick="closeAssistantModal()" class="w-full bg-emerald-600 hover:bg-emerald-700 text-white font-extrabold py-4 rounded-2xl transition-all shadow-lg hover:shadow-emerald-200/50 active:scale-[0.98]">
          I UNDERSTAND
        </button>
      </div>
    </div>
  </div>
  
  <!-- Dynamic Background Blobs -->
  <div class="fixed top-[-10%] left-[-10%] w-[40%] h-[40%] bg-emerald-200/40 rounded-full blur-[120px] animate-blob -z-10"></div>
  <div class="fixed bottom-[-10%] right-[-10%] w-[40%] h-[40%] bg-green-200/40 rounded-full blur-[120px] animate-blob animation-delay-2000 -z-10"></div>

  <div class="max-w-3xl w-full">
    <!-- Branding & Header -->
    <div class="text-center mb-6 md:mb-12">
      <h1 class="text-4xl sm:text-6xl md:text-8xl font-black tracking-tighter mb-4 md:mb-6 relative group select-none">
        <span class="absolute -inset-2 bg-emerald-400 blur-2xl opacity-20 group-hover:opacity-30 transition-opacity"></span>
        <span class="relative bg-clip-text text-transparent bg-gradient-to-r from-gray-900 via-emerald-600 to-gray-900 animate-gradient-x">DHNET</span><span class="relative text-emerald-500 drop-shadow-sm">4U</span>
      </h1>
      <div class="max-w-xl mx-auto bg-emerald-50/50 backdrop-blur-sm p-5 sm:p-6 rounded-2xl border border-emerald-100/50 shadow-sm mx-1 sm:mx-auto">
        <p class="text-gray-700 text-xs sm:text-sm md:text-base leading-relaxed font-medium">
          Welcome to <span class="font-bold text-emerald-800">DHNET4U Support</span>. We are dedicated to ensuring your data delivery is seamless. If you encounter any issues, please provide your details below for prioritized resolution.
        </p>
      </div>
    </div>

    <!-- Main Card -->
    <div class="glass-card rounded-[1.5rem] sm:rounded-[2rem] md:rounded-[2.5rem] p-4 sm:p-8 md:p-16 border border-white/50 relative overflow-hidden mx-1 md:mx-0">
      <!-- Top Accent Line -->
      <div class="absolute top-0 left-0 w-full h-1.5 bg-gradient-to-r from-emerald-400 via-green-500 to-emerald-600"></div>

      <form id="dhnet4u-form" class="space-y-6 md:space-y-10">
        <!-- Report Type Selection -->
        <div class="space-y-4">
          <label class="block text-center text-[11px] sm:text-sm font-black uppercase tracking-[0.2em] text-emerald-900/70">Select Report Type</label>
          <div class="grid grid-cols-3 gap-2 sm:gap-4">
            <button type="button" onclick="setReportType('bundle')" id="tab-bundle" class="report-tab active flex flex-col sm:flex-row items-center justify-center sm:space-x-2 px-2 sm:px-6 py-3.5 sm:py-4 rounded-xl md:rounded-2xl border-2 transition-all font-bold text-[12px] sm:text-sm">
              <svg class="w-4 h-4 md:w-5 md:h-5 mb-1 sm:mb-0" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M20 7l-8-4-8 4m16 0l-8 4m8-4v10l-8 4m0-10L4 7m8 4v10M4 7v10l8 4"></path></svg>
              <span>Bundle</span>
            </button>
            <button type="button" onclick="setReportType('afa')" id="tab-afa" class="report-tab flex flex-col sm:flex-row items-center justify-center sm:space-x-2 px-2 sm:px-6 py-3.5 sm:py-4 rounded-xl md:rounded-2xl border-2 transition-all font-bold text-[12px] sm:text-sm">
              <svg class="w-4 h-4 md:w-5 md:h-5 mb-1 sm:mb-0" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 9v2m0 4h.01m-6.938 4h13.856c1.54 0 2.502-1.667 1.732-3L13.732 4c-.77-1.333-2.694-1.333-3.464 0L3.34 16c-.77 1.333.192 3 1.732 3z"></path></svg>
              <span>AFA</span>
            </button>
            <button type="button" onclick="setReportType('suggestion')" id="tab-suggestion" class="report-tab flex flex-col sm:flex-row items-center justify-center sm:space-x-2 px-2 sm:px-6 py-3.5 sm:py-4 rounded-xl md:rounded-2xl border-2 transition-all font-bold text-[12px] sm:text-sm">
              <svg class="w-4 h-4 md:w-5 md:h-5 mb-1 sm:mb-0" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M7 8h10M7 12h4m1 8l-4-4H5a2 2 0 01-2-2V6a2 2 0 012-2h14a2 2 0 012 2v8a2 2 0 01-2 2h-3l-4 4z"></path></svg>
              <span>Idea</span>
            </button>
          </div>
          <input type="hidden" name="report_type" id="report_type" value="bundle">
        </div>

        <div id="form-fields" class="space-y-5 md:space-y-8 animate-fade-in">
          <div id="phone-field" class="space-y-3">
            <label class="flex items-center text-sm font-bold text-gray-800 space-x-2">
              <svg class="w-4 h-4 md:w-5 md:h-5 text-emerald-600" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 5a2 2 0 012-2h3.28a1 1 0 01.948.684l1.498 4.493a1 1 0 01-.502 1.21l-2.257 1.13a11.042 11.042 0 005.516 5.516l1.13-2.257a1 1 0 011.21-.502l4.493 1.498a1 1 0 01.684.949V19a2 2 0 01-2 2h-1C9.716 21 3 14.284 3 6V5z"></path></svg>
              <span>Phone Number</span>
            </label>
            <input type="tel" name="number" placeholder="e.g. 024XXXXXXX" required 
              pattern="0[0-9]{9}" title="Valid 10-digit Ghana number"
              class="w-full bg-white border-2 border-gray-100 text-base rounded-xl md:rounded-2xl block p-4 input-field outline-none">
          </div>

          <div id="bundle-fields" class="grid grid-cols-1 md:grid-cols-2 gap-6 md:gap-8">
            <div class="space-y-3">
              <label class="flex items-center text-sm font-bold text-gray-800 space-x-2">
                <svg class="w-4 h-4 md:w-5 md:h-5 text-emerald-600" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8.111 16.404a5.5 5.5 0 017.778 0M12 20h.01m-7.08-7.071a9.5 9.5 0 0114.142 0M6.25 12c.325.325.617.674.873 1.045m10.627-1.045a10.963 10.963 0 00-.873 1.045M12 4v2"></path></svg>
                <span>Network Provider</span>
              </label>
              <div class="relative group">
                <select name="network" id="network-select" class="w-full appearance-none bg-white border-2 border-gray-100 text-base rounded-xl md:rounded-2xl block p-4 input-field outline-none">
                  <option value="" disabled selected>Select Network</option>
                  <option value="MTN">MTN</option>
                  <option value="Telecel">Telecel</option>
                  <option value="AT">AT</option>
                </select>
                <div class="absolute inset-y-0 right-0 flex items-center pr-4 pointer-events-none text-emerald-500 group-focus-within:rotate-180 transition-transform">
                  <svg class="w-4 h-4 md:w-5 md:h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2.5" d="M19 9l-7 7-7-7"></path></svg>
                </div>
              </div>
            </div>

            <div class="space-y-3">
              <label class="flex items-center text-sm font-bold text-gray-800 space-x-2">
                <svg class="w-4 h-4 md:w-5 md:h-5 text-emerald-600" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M20 7l-8-4-8 4m16 0l-8 4m8-4v10l-8 4m0-10L4 7m8 4v10M4 7v10l8 4"></path></svg>
                <span>Data Package</span>
              </label>
              <div class="relative group">
                <select id="package-select" name="package" class="w-full appearance-none bg-white border-2 border-gray-100 text-base rounded-xl md:rounded-2xl block p-4 input-field outline-none">
                  <option value="" disabled selected>Select Gigs</option>
                </select>
                <div class="absolute inset-y-0 right-0 flex items-center pr-4 pointer-events-none text-emerald-500 group-focus-within:rotate-180 transition-transform">
                  <svg class="w-4 h-4 md:w-5 md:h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2.5" d="M19 9l-7 7-7-7"></path></svg>
                </div>
              </div>
            </div>

            <div class="space-y-3">
              <label class="flex items-center text-sm font-bold text-gray-800 space-x-2">
                <svg class="w-4 h-4 md:w-5 md:h-5 text-emerald-600" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"></path></svg>
                <span>Transaction Status</span>
              </label>
              <div class="relative group">
                <select name="status" id="status-select" class="w-full appearance-none bg-white border-2 border-gray-100 text-base rounded-xl md:rounded-2xl block p-4 input-field outline-none">
                  <option value="" disabled selected>Select Status</option>
                  <option value="Completed">Completed</option>
                  <option value="Pending">Pending</option>
                  <option value="Failed">Failed</option>
                </select>
                <div class="absolute inset-y-0 right-0 flex items-center pr-4 pointer-events-none text-emerald-500 group-focus-within:rotate-180 transition-transform">
                  <svg class="w-4 h-4 md:w-5 md:h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2.5" d="M19 9l-7 7-7-7"></path></svg>
                </div>
              </div>
            </div>

            <div class="space-y-3">
              <label class="flex items-center text-sm font-bold text-gray-800 space-x-2">
                <svg class="w-4 h-4 md:w-5 md:h-5 text-emerald-600" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 5v2m0 4v2m0 4v2M5 5a2 2 0 012-2h10a2 2 0 012 2v14a2 2 0 01-2 2H7a2 2 0 01-2-2V5z"></path></svg>
                <span>Transaction ID <span class="text-[10px] font-normal opacity-60 ml-1">(Optional)</span></span>
              </label>
              <input type="text" name="transaction" placeholder="e.g. 76079202593" 
                pattern="[0-9]{11}" title="11-digit Paystack ID"
                class="w-full bg-white border-2 border-gray-100 text-base rounded-xl md:rounded-2xl block p-4 input-field outline-none">
            </div>

          </div>

          <div id="date-field" class="space-y-3">
            <label class="flex items-center text-sm font-bold text-gray-800 space-x-2">
              <svg class="w-4 h-4 md:w-5 md:h-5 text-emerald-600" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 7V3m8 4V3m-9 8h10M5 21h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v12a2 2 0 002 2z"></path></svg>
              <span>Date of Event</span>
            </label>
            <input type="datetime-local" name="date" id="date-input" 
              class="w-full bg-white border-2 border-gray-100 text-base rounded-xl md:rounded-2xl block p-4 input-field outline-none">
          </div>

          <!-- AFA Specific Field -->
          <div id="afa-fields" class="hidden space-y-3">
            <label class="flex items-center text-sm font-bold text-gray-800 space-x-2">
              <svg class="w-4 h-4 md:w-5 md:h-5 text-emerald-600" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 9v2m0 4h.01m-6.938 4h13.856c1.54 0 2.502-1.667 1.732-3L13.732 4c-.77-1.333-2.694-1.333-3.464 0L3.34 16c-.77 1.333.192 3 1.732 3z"></path></svg>
              <span>AFA REPORT <span class="text-[10px] font-normal opacity-60 ml-1">(Registral number)</span></span>
            </label>
            <input type="tel" name="afa_report" id="afa-input" placeholder="e.g. 024XXXXXXX" 
              pattern="0[0-9]{9}" title="Valid 10-digit Ghana number"
              class="w-full bg-white border-2 border-gray-100 text-base rounded-xl md:rounded-2xl block p-4 input-field outline-none">
          </div>

          <!-- Screenshot Upload Section -->
          <div id="screenshot-section" class="md:col-span-2 space-y-4 pt-2">
            <div class="bg-emerald-50/80 border border-emerald-100 rounded-2xl p-4 flex items-start space-x-3">
              <svg class="w-5 h-5 text-emerald-600 mt-0.5 shrink-0" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 16h-1v-4h-1m1-4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z"></path></svg>
              <p class="text-[11px] sm:text-xs text-emerald-800 font-semibold leading-relaxed">
                TIP: Uploading a screenshot of your <span class="underline">current data balance</span> or <span class="underline">payment confirmation</span> helps us resolve your issue significantly faster.
              </p>
            </div>
            
            <div class="grid grid-cols-1 gap-4">
              <div class="space-y-2">
                <label class="flex items-center text-[11px] font-bold text-gray-800 uppercase tracking-wider space-x-2">
                  <svg class="w-4 h-4 text-emerald-600" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 9a2 2 0 012-2h.93a2 2 0 001.664-.89l.812-1.22A2 2 0 0110.07 4h3.86a2 2 0 011.664.89l.812 1.22A2 2 0 0018.07 7H19a2 2 0 012 2v9a2 2 0 01-2 2H5a2 2 0 01-2-2V9z"></path><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 13a3 3 0 11-6 0 3 3 0 016 0z"></path></svg>
                  <span>Upload Screenshot <span class="text-[9px] font-normal opacity-60 lowercase">(Optional)</span></span>
                </label>
                <div class="relative group">
                  <input type="file" name="screenshot" id="screenshot" accept="image/*" class="hidden">
                  <label for="screenshot" class="w-full flex items-center justify-between bg-white border-2 border-dashed border-gray-200 hover:border-emerald-400 p-3 rounded-xl cursor-pointer transition-all group-hover:bg-emerald-50/30">
                    <span class="text-xs text-gray-500 truncate pr-2" id="screenshot-name">Choose screenshot...</span>
                    <span class="bg-gray-100 text-[10px] font-bold px-3 py-1 rounded-lg group-hover:bg-emerald-600 group-hover:text-white transition-colors">BROWSE</span>
                  </label>
                </div>
              </div>
            </div>
          </div>

          <!-- Message Field (Always Last) -->
          <div class="space-y-3">
            <label class="flex items-center text-sm font-bold text-gray-800 space-x-2">
              <svg class="w-4 h-4 md:w-5 md:h-5 text-emerald-600" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M7 8h10M7 12h4m1 8l-4-4H5a2 2 0 01-2-2V6a2 2 0 012-2h14a2 2 0 012 2v8a2 2 0 01-2 2h-3l-4 4z"></path></svg>
              <span id="message-label" class="flex items-center">Additional Message <span class="text-[10px] font-normal opacity-60 ml-1">(Optional)</span></span>
            </label>
            <textarea name="message" id="message-input" rows="4" placeholder="Describe your issue or suggestion..." 
              class="w-full bg-white border-2 border-gray-100 text-base rounded-xl md:rounded-2xl block p-4 input-field outline-none resize-none transition-all"></textarea>
            <div id="char-limit-warning" class="hidden text-[10px] text-amber-600 font-bold flex items-center space-x-1 animate-pulse">
              <svg class="w-3 h-3" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 9v2m0 4h.01m-6.938 4h13.856c1.54 0 2.502-1.667 1.732-3L13.732 4c-.77-1.333-2.694-1.333-3.464 0L3.34 16c-.77 1.333.192 3 1.732 3z"></path></svg>
              <span id="warning-text">Kindly keep your message concise for better resolution.</span>
            </div>
          </div>
        </div>

        <!-- Submit Button -->
        <div class="pt-4">
          <button type="submit" id="submit-btn" class="w-full pro-green-btn text-white text-base md:text-lg font-bold py-4 md:py-5 px-6 md:px-8 rounded-2xl md:rounded-3xl flex items-center justify-center space-x-3 group active:scale-[0.98] transition-transform">
            <span id="btn-text">Submit Report</span>
            <svg id="btn-icon" class="w-5 h-5 md:w-6 md:h-6 group-hover:translate-x-1 transition-transform" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2.5" d="M14 5l7 7m0 0l-7 7m7-7H3"></path>
            </svg>
          </button>
        </div>
      </form>

      <!-- Results Container -->
      <div id="status-container" class="mt-6 md:mt-10 hidden transform transition-all duration-500">
        <div id="status-msg" class="p-4 md:p-6 rounded-2xl md:rounded-3xl text-center font-bold border-2 text-sm md:text-base"></div>
      </div>
    </div>

    <!-- Support Features (Bottom) -->
    <div class="mt-8 md:mt-12 flex flex-col sm:flex-row items-center justify-center gap-6 md:gap-12 px-2 md:px-6">
      <div class="flex items-center space-x-3 group">
        <div class="w-8 h-8 bg-emerald-100 rounded-lg flex items-center justify-center group-hover:bg-emerald-200 transition-colors">
          <svg class="w-4 h-4 text-emerald-600" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4l3 3m6-3a9 9 0 11-18 0 9 9 0 0118 0z"></path></svg>
        </div>
        <div class="text-left">
          <h4 class="text-[9px] font-black uppercase tracking-widest text-emerald-900">Priority Resolution</h4>
          <p class="text-[8px] text-emerald-700/60 leading-tight">Dedicated support for every report.</p>
        </div>
      </div>

      <div class="h-8 w-px bg-emerald-100 hidden sm:block"></div>

      <div class="flex items-center space-x-3 group">
        <div class="w-8 h-8 bg-emerald-100 rounded-lg flex items-center justify-center group-hover:bg-emerald-200 transition-colors">
          <svg class="w-4 h-4 text-emerald-600" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 10V3L4 14h7v7l9-11h-7z"></path></svg>
        </div>
        <div class="text-left">
          <h4 class="text-[9px] font-black uppercase tracking-widest text-emerald-900">Real-time Monitoring</h4>
          <p class="text-[8px] text-emerald-700/60 leading-tight">24/7 delivery log verification.</p>
        </div>
      </div>
    </div>

    <!-- World Class Footer -->
    <div class="mt-6 md:mt-8 text-center px-4">
      <div class="flex items-center justify-center space-x-4 mb-4">
        <span class="h-px w-8 md:w-12 bg-emerald-200"></span>
        <span class="text-emerald-700 font-bold text-[9px] md:text-[10px] uppercase tracking-[0.2em]">Verified Secure Portal</span>
        <span class="h-px w-8 md:w-12 bg-emerald-200"></span>
      </div>
      <p class="text-gray-400 text-[8px] md:text-[9px] tracking-wider uppercase font-bold">&copy; 2026 DHNET4U Data Solutions.</p>
    </div>
  </div>

  <script>
    // Initialize elements with a helper to avoid crashes
    let elements = {};
    
    function initElements() {
      try {
        elements = {
          reportType: document.getElementById('report_type'),
          tabs: document.querySelectorAll('.report-tab'),
          phoneField: document.getElementById('phone-field'),
          bundleFields: document.getElementById('bundle-fields'),
          afaFields: document.getElementById('afa-fields'),
          dateField: document.getElementById('date-field'),
          messageLabel: document.getElementById('message-label'),
          messageInput: document.getElementById('message-input'),
          afaInput: document.getElementById('afa-input'),
          dateInput: document.getElementById('date-input'),
          phoneInput: document.querySelector('#phone-field input'),
          bundleInputs: document.querySelectorAll('#bundle-fields select, #bundle-fields input'),
          packageSelect: document.getElementById('package-select'),
          form: document.getElementById("dhnet4u-form"),
          statusContainer: document.getElementById("status-container"),
          statusMsg: document.getElementById("status-msg"),
          submitBtn: document.getElementById("submit-btn"),
          btnText: document.getElementById("btn-text"),
          btnIcon: document.getElementById("btn-icon"),
          charWarning: document.getElementById("char-limit-warning"),
          warningText: document.getElementById("warning-text"),
          screenshotSection: document.getElementById("screenshot-section"),
          screenshotInput: document.getElementById("screenshot"),
          screenshotName: document.getElementById("screenshot-name")
        };
        console.log("DHNET4U: Elements initialized successfully.");
        return true;
      } catch (err) {
        console.error("DHNET4U: Element initialization failed:", err);
        return false;
      }
    }

    // Helper to convert and compress image to base64
    const processImage = file => new Promise((resolve, reject) => {
      const reader = new FileReader();
      reader.readAsDataURL(file);
      reader.onload = event => {
        const img = new Image();
        img.src = event.target.result;
        img.onload = () => {
          const canvas = document.createElement('canvas');
          const MAX_WIDTH = 800; // Aggressive compression to fit in Sheet cell (max 50k chars)
          const MAX_HEIGHT = 800;
          let width = img.width;
          let height = img.height;

          if (width > height) {
            if (width > MAX_WIDTH) {
              height *= MAX_WIDTH / width;
              width = MAX_WIDTH;
            }
          } else {
            if (height > MAX_HEIGHT) {
              width *= MAX_HEIGHT / height;
              height = MAX_HEIGHT;
            }
          }

          canvas.width = width;
          canvas.height = height;
          const ctx = canvas.getContext('2d');
          ctx.drawImage(img, 0, 0, width, height);
          
          // Compress to JPEG with 0.4 quality for very light size
          const dataUrl = canvas.toDataURL('image/jpeg', 0.4);
          resolve(dataUrl.split(',')[1]);
        };
      };
      reader.onerror = error => reject(error);
    });


    // Optimized Report Type Switching
    function setReportType(type) {
      if (!elements.reportType) return;
      
      elements.reportType.value = type;
      
      // Update Tabs UI
      elements.tabs.forEach(tab => {
        tab.classList.toggle('active', tab.id === `tab-${type}`);
      });
      
      // Reset required attributes
      if (elements.phoneInput) elements.phoneInput.required = false;
      elements.bundleInputs.forEach(input => input.required = false);
      if (elements.afaInput) elements.afaInput.required = false;
      if (elements.dateInput) elements.dateInput.required = false;
      if (elements.messageInput) elements.messageInput.required = false;

      // Efficient Field Toggling
      if (type === 'bundle') {
        elements.phoneField?.classList.remove('hidden');
        elements.bundleFields?.classList.remove('hidden');
        elements.afaFields?.classList.add('hidden');
        elements.dateField?.classList.remove('hidden');
        elements.screenshotSection?.classList.remove('hidden');
        if (elements.messageLabel) elements.messageLabel.innerHTML = 'Additional Message <span class="text-sm font-normal opacity-60 ml-1">(Optional)</span>';
        if (elements.messageInput) elements.messageInput.placeholder = "Briefly describe your issue...";
        
        if (elements.phoneInput) elements.phoneInput.required = true;
        elements.bundleInputs.forEach(input => {
          if (input.name !== 'transaction') input.required = true;
        });
        if (elements.dateInput) elements.dateInput.required = true;
      } else if (type === 'afa') {
        elements.phoneField?.classList.add('hidden');
        elements.bundleFields?.classList.add('hidden');
        elements.afaFields?.classList.remove('hidden');
        elements.dateField?.classList.add('hidden');
        elements.screenshotSection?.classList.add('hidden');
        if (elements.messageLabel) elements.messageLabel.innerHTML = 'Additional Message <span class="text-sm font-normal opacity-60 ml-1">(Optional)</span>';
        if (elements.messageInput) elements.messageInput.placeholder = "Any additional details for the AFA report...";
        if (elements.afaInput) elements.afaInput.required = true;
      } else {
        elements.phoneField?.classList.add('hidden');
        elements.bundleFields?.classList.add('hidden');
        elements.afaFields?.classList.add('hidden');
        elements.dateField?.classList.add('hidden');
        elements.screenshotSection?.classList.add('hidden');
        if (elements.messageLabel) elements.messageLabel.innerHTML = 'Your Suggestion / Message <span class="text-sm font-bold text-emerald-700 ml-1">(Required)</span>';
        if (elements.messageInput) {
          elements.messageInput.placeholder = "We value your feedback. Please let us know how we can improve...";
          elements.messageInput.required = true;
        }
      }
    }

    // Assistant Modal Logic
    function openAssistantModal() {
      const modal = document.getElementById('pro-assistant-modal');
      modal.classList.remove('hidden');
      setTimeout(() => {
        modal.classList.add('opacity-100');
        modal.querySelector('.bg-white').classList.remove('scale-95');
        modal.querySelector('.bg-white').classList.add('scale-100');
      }, 100);
    }

    function closeAssistantModal() {
      const modal = document.getElementById('pro-assistant-modal');
      modal.classList.remove('opacity-100');
      modal.querySelector('.bg-white').classList.add('scale-95');
      setTimeout(() => {
        modal.classList.add('hidden');
      }, 500);
    }

    // Initialize everything when the DOM is ready
    document.addEventListener('DOMContentLoaded', () => {
      if (!initElements()) return;
      
      // Open World-Class Assistant Pop-up on load
      setTimeout(openAssistantModal, 800);

      // Character Limit Handling
      elements.messageInput?.addEventListener('input', (e) => {
        const type = elements.reportType.value;
        const limit = type === 'suggestion' ? 300 : 200;
        const currentLen = e.target.value.length;
        
        if (currentLen > limit) {
          e.target.value = e.target.value.substring(0, limit);
          elements.warningText.textContent = `Kindly respect the ${limit} character limit for our team. Thank you!`;
          elements.charWarning.classList.remove('hidden');
          e.target.classList.add('border-amber-400');
        } else {
          elements.charWarning.classList.add('hidden');
          e.target.classList.remove('border-amber-400');
        }
      });

      // File name update listener
      elements.screenshotInput?.addEventListener('change', e => {
        elements.screenshotName.textContent = e.target.files[0] ? e.target.files[0].name : "Choose screenshot...";
      });

      // Generate 1GB to 100GB once
      const fragment = document.createDocumentFragment();
      for (let i = 1; i <= 100; i++) {
        const opt = document.createElement('option');
        opt.value = `${i}GB`;
        opt.textContent = `${i}GB Package`;
        fragment.appendChild(opt);
      }
      elements.packageSelect?.appendChild(fragment);
      
      // Set initial state
      setReportType('bundle');

      // Form submission handler
      elements.form?.addEventListener("submit", async (e) => {
        e.preventDefault();
        
        console.log("DHNET4U: Form submission started.");
        elements.submitBtn.disabled = true;
        elements.btnText.textContent = "Processing Securely...";
        elements.btnIcon.classList.add('animate-pulse');
        elements.statusContainer.classList.add("hidden");

        try {
          const formData = new FormData(elements.form);
          const data = Object.fromEntries(formData.entries());

          // Handle file upload with compression
          if (elements.screenshotInput?.files[0]) {
            console.log("DHNET4U: Optimizing and processing screenshot...");
            elements.btnText.textContent = "Optimizing Image...";
            data.screenshot = await processImage(elements.screenshotInput.files[0]);
          }

          const params = new URLSearchParams(data).toString();
          const scriptURL = "https://script.google.com/macros/s/AKfycbzB9AagCeNHwttlct0GrDyp_LxBeqIUYI83O9LZH_IoMVInflMXBNhXVnqfZ6wF2Ro/exec";

          console.log("DHNET4U: Sending data to GAS...");
          const response = await fetch(scriptURL, {
            method: "POST",
            body: params,
            headers: { "Content-Type": "application/x-www-form-urlencoded" }
          });

          const result = await response.json();
          console.log("DHNET4U: Server response:", result);
          
          elements.statusContainer.classList.remove("hidden");
          if(result.result === "success") {
            elements.statusMsg.className = "p-6 rounded-3xl text-center font-bold bg-emerald-50 text-emerald-700 border-emerald-200 shadow-sm animate-bounce";
            elements.statusMsg.innerHTML = '<div class="flex flex-col items-center space-y-2"><svg class="w-10 h-10" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2.5" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"></path></svg><span class="text-lg">SUBMITTED SUCCESSFULLY!</span></div>';
            
            // Fast standard reset
            elements.form.reset();
            if (elements.screenshotName) elements.screenshotName.textContent = "Choose screenshot...";
            setReportType('bundle'); // Reset to default
            
            // Auto-hide success message after 4 seconds to keep site clean
            setTimeout(() => {
              elements.statusContainer.classList.add("hidden");
            }, 4000);
          } else if(result.result === "duplicate") {
            elements.statusMsg.className = "p-6 rounded-3xl text-center font-bold bg-amber-50 text-amber-700 border-amber-200 shadow-sm";
            elements.statusMsg.innerHTML = `<div class="flex flex-col items-center space-y-2"><svg class="w-10 h-10" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2.5" d="M12 9v2m0 4h.01m-6.938 4h13.856c1.54 0 2.502-1.667 1.732-3L13.732 4c-.77-1.333-2.694-1.333-3.464 0L3.34 16c-.77 1.333.192 3 1.732 3z"></path></svg><span>NOTICE: ${result.message.toUpperCase()}</span></div>`;
          } else {
            throw new Error(result.error || "Unknown server error");
          }
        } catch(err) {
          console.error("DHNET4U: Submission error:", err);
          elements.statusContainer.classList.remove("hidden");
          elements.statusMsg.className = "p-6 rounded-3xl text-center font-bold bg-rose-50 text-rose-700 border-rose-200 shadow-sm";
          elements.statusMsg.textContent = "SYSTEM ERROR: " + (err.message.includes("fetch") ? "CONNECTION LOST" : "PLEASE TRY AGAIN");
        } finally {
          elements.submitBtn.disabled = false;
          elements.btnText.textContent = "Submit Report";
          elements.btnIcon.classList.remove('animate-pulse');
        }
      });
    });
  </script>
</body>
</html>
